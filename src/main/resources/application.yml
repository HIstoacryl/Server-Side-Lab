spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: Wswpr1230

  redis:
    host: localhost
    port: 6379
    database: 0

  ai:
    dashscope:
      api-key: "fake-key-for-startup" # 本地启动用，后续替换为真实密钥
      chat:
        options:
          model: qwen-turbo # 通义千问模型，可切换qwen-plus等

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

server:
  port: 8080

jwt:
  secret: demo-jwt-secret-key-for-spring-security-2026
  expiration-millis: 7200000